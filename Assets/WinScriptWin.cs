using UnityEngine;
using UnityEngine.SceneManagement;

public class WinScriptWin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SceneManager.LoadScene("WinStage");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
